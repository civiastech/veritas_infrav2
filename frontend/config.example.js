window.VERITAS_CONFIG = {
  // For same-domain deploys behind Nginx or a reverse proxy, keep '/api/v1'
  // For split frontend/backend deploys, use the full backend base, e.g.
  // API_BASE: 'https://your-backend.example.com/api/v1'
  API_BASE: '/api/v1'
};
