
POSTGRES_DB=veritas
POSTGRES_USER=veritas
POSTGRES_PASSWORD=veritas_change_me
JWT_SECRET_KEY=change-this-in-production
FIRST_SUPERUSER_EMAIL=admin@visc.org
FIRST_SUPERUSER_PASSWORD=AdminPass123!
CORS_ORIGINS=http://localhost,http://127.0.0.1
TRUSTED_HOSTS=localhost,127.0.0.1
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET_EVIDENCE=veritas-evidence
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin_change_me


# EXTERNAL DATABASE (Supabase / Neon / any managed Postgres)
# Replace DATABASE_URL with your hosted Postgres connection string when not using local docker Postgres.
# Example: postgresql+psycopg://USER:PASSWORD@HOST:6543/postgres?sslmode=require
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:6543/postgres?sslmode=require

# FRONTEND
# For split frontend/backend hosting, set config.js API_BASE to your backend URL + /api/v1
